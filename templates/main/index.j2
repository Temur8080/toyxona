{% extends 'layouts/base.j2' %}

{% block content %}
<div class="landing-page">
    <div class="landing-logo"><i class="bi bi-grid-1x2-fill"></i></div>
    <h1 class="fw-semibold mb-2">Toyxona</h1>
    <p class="text-muted mb-3 mx-auto" style="max-width: 420px;">
        {{ _("Toyxona zallari uchun zamonaviy odamlar soni va kamera monitoring tizimi") }}
    </p>
    <a href="{{ url('account:login') }}" class="btn btn-primary">
        <i class="bi bi-box-arrow-in-right me-2"></i>{{ _("Tizimga kirish") }}
    </a>
</div>
{% endblock %}
