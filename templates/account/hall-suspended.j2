{% extends 'layouts/base.j2' %}

{% block content %}
<div class="landing-page">
    <div class="landing-logo"><i class="bi bi-pause-circle"></i></div>
    <h1 class="h3 fw-bold mb-3">{{ _("Toyxona vaqtincha to'xtatilgan") }}</h1>
    <p class="text-muted mb-4">{{ _("Iltimos, administrator bilan bog'laning.") }}</p>
    <a href="{{ url('account:logout') }}" class="btn btn-outline-primary">{{ _("Chiqish") }}</a>
</div>
{% endblock %}
